<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.InnerCircle" parent="android:Theme.Material.Light.NoActionBar">
        <!-- The first screen is the splash, so the launch window is the splash's grey: no cream or white
             flash before Compose draws it. Every other screen paints its own background. -->
        <item name="android:windowBackground">#FFC2BFBB</item>
    </style>
</resources>
